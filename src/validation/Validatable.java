//Created by Shivom Bhanot (1319202)

package validation;

// Interface for any object that can validate itself
public interface Validatable {

    // Each class implementing this must define its own validation logic
    void validate();
}